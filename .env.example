# ============================================
# Heyapply - Environment Variables
# ============================================
# Kopiere diese Datei zu .env.local und fülle die Werte aus
# cp .env.example .env.local

# ============================================
# ERFORDERLICH FÜR MVP
# ============================================

# Database (SQLite - funktioniert out-of-the-box, keine externe DB nötig)
# Für Production: PostgreSQL (z.B. Supabase)
DATABASE_URL="file:./prisma/dev.db"

# NextAuth - Session Management
# Generiere ein sicheres Secret mit: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generiere-ein-sicheres-secret-mit-openssl-rand-base64-32"

# Anthropic Claude API - ERFORDERLICH für CV/Anschreiben-Generierung
# Kosten: Pay-as-you-go (~$0.25 pro 1M Input Tokens, sehr günstig für MVP)
# Registrierung: https://console.anthropic.com/
ANTHROPIC_API_KEY="sk-ant-..."

# ============================================
# OPTIONAL - Für vollständige Funktionalität
# ============================================

# Supabase - File Storage (CVs, Profilbilder)
# Free Tier: 500MB Storage, 2GB Bandwidth/Monat
# Registrierung: https://supabase.com/
# Setup: Erstelle Buckets "cvs" und "profile-images" in Supabase Dashboard
NEXT_PUBLIC_SUPABASE_URL="https://xxx.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."

# Resend - E-Mail Versand (Verifizierung, Passwort-Reset)
# Free Tier: 100 E-Mails/Tag, 3000/Monat
# Registrierung: https://resend.com/
# Ohne Resend: E-Mail-Verifizierung wird übersprungen (User kann direkt einloggen)
RESEND_API_KEY="re_..."
EMAIL_FROM="Heyapply <noreply@heyapply.ch>"

# LinkedIn OAuth - Profil-Import
# Kostenlos, keine Limits
# Registrierung: https://www.linkedin.com/developers/apps
# Redirect URI: ${NEXTAUTH_URL}/api/profile/linkedin?action=callback
LINKEDIN_CLIENT_ID="..."
LINKEDIN_CLIENT_SECRET="..."

# Stripe - Zahlungen (Subscription Management)
# Test Mode: Kostenlos, unbegrenzt
# Registrierung: https://stripe.com/
# Setup: Erstelle Products & Prices im Stripe Dashboard
# Webhook Endpoint: ${NEXTAUTH_URL}/api/stripe/webhook
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
STRIPE_PRICE_BASIS="price_..."  # Price ID für Basis-Abo (CHF 9.99/Monat)
STRIPE_PRICE_PRO="price_..."    # Price ID für Pro-Abo (CHF 19.99/Monat)

# ============================================
# MVP MINIMUM SETUP
# ============================================
# Für ein funktionierendes MVP benötigst du mindestens:
# 1. DATABASE_URL (SQLite funktioniert out-of-the-box)
# 2. NEXTAUTH_URL und NEXTAUTH_SECRET
# 3. ANTHROPIC_API_KEY (für AI-Features)
#
# Optional aber empfohlen:
# - Supabase (für File Uploads)
# - Resend (für E-Mail-Verifizierung)
#
# Ohne Supabase: CV/Profilbild-Upload funktioniert nicht
# Ohne Resend: E-Mail-Verifizierung wird übersprungen
# Ohne LinkedIn: LinkedIn-Import funktioniert nicht
# Ohne Stripe: Zahlungen funktionieren nicht (aber App läuft)

# ============================================
# OAUTH PROVIDERS - Social Login
# ============================================

# Google OAuth - Login mit Google
# Kostenlos
# Registrierung: https://console.cloud.google.com/apis/credentials
# Redirect URI: ${NEXTAUTH_URL}/api/auth/callback/google
GOOGLE_CLIENT_ID="..."
GOOGLE_CLIENT_SECRET="..."

# LinkedIn OAuth - Login mit LinkedIn (zusätzlich zum Profil-Import)
# Kostenlos
# Registrierung: https://www.linkedin.com/developers/apps
# Redirect URI: ${NEXTAUTH_URL}/api/auth/callback/linkedin
# LINKEDIN_CLIENT_ID und LINKEDIN_CLIENT_SECRET oben verwenden
